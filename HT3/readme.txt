// Список свойств и методов
// * - дополнительные методы, отсутствующие в ДЗ

Chat:

Chat.chats                                    // массив чатов

Chat.getChats()                               // возвращает массив чатов
Chat.showChats([user])                        // * выводит в консоль список чатов с количеством собщений и количеством непрочитанных
                                              //   пользователем собщений
Chat.getChatByName(chatName)                  // * возвращает инстанс  чата по заданному имени

this.name                                     // имя чата
this.history                                  // массив сообщений в чате
this.loggedUsers                              // массив подключенных к чату пользователями

this.login([user1[, user2[, ...]]])           // подключает пользователей к чату
this.logoff([user1[, user2[, ...]]])          // отключает пользователей от чата
this.sendMessage(user, messageText)           // создает сообщение в истории чата, выводит созданное сообщение
this.showHistory([[index,] number])           // выводит в лог сообщения из истории чата
this.readMessages(user[, numberOfMessages])   // "читает" сообщения из истории и возвращает массив прочитанных
this.logMessage(message)                      // выводит в лог сообщениe
this.countUnreaded(user)                      // возвращает количество непрочитанных пользователем сообщений в чате

------------------------------------------    //
Message                                       //

this.time                                     // время создания сообщения
this.user                                     // автор сообщения
this.text                                     // текст сообщения
this.readBy                                   // массив прочитавших сообщение

this.read(user)                               // "читает" сообщение
this.getTimeToLog()                           // возвращает время создания сообщения в заданном формате

------------------------------------------    //
User                                          //

User.users                                    // массив пользователей

User.showUsers([userName])                    // * выводит в консоль список пользователей с указанным именем
User.getUserbyId(id)                          // * возвращает пользователя по id

this.setDefaultChat(chat)                     // устанавливает чат по умолчанию
this.login([chat])                            // подключает пользователя к чату
this.logoff([chat])                           // отключает пользователя от чата
this.sendMessage([chat,] messageText)         // отсылает сообщение
this.readMessages([chat],[numberOfMessages])  // "читает" сообщения из истории чата и выводит их в консоль
